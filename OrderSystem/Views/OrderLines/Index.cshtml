@model IEnumerable<OrderSystem.Data.OrderLines>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Item)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrdernNumberNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Item.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrdernNumberNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LineNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LineNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LineNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
